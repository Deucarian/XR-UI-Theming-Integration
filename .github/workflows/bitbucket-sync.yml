name: Pull package updates from Bitbucket

on:
  workflow_dispatch:
  schedule:
    - cron: "55 * * * *"
  workflow_run:
    workflows: ["Package Validation"]
    types: [completed]

permissions:
  contents: write
  pull-requests: write
  actions: write

concurrency:
  group: bitbucket-package-synchronization
  cancel-in-progress: false

jobs:
  synchronize:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    uses: Deucarian/Package-Registry/.github/workflows/package-synchronization.yml@d018d78b90907c594baf3b95f613b43a71cab828
    with:
      engine-ref: d018d78b90907c594baf3b95f613b43a71cab828
    secrets:
      BITBUCKET_READ_KEY: ${{ secrets.BITBUCKET_READ_KEY }}
